{
	"name": "ssh-plugin",
	"private": true,
	"type": "module",
	"description": "pi-web-ui SSH 远程管理插件（构建用 package.json，不发布；运行时依赖 ssh2 由插件服务端自动安装）",
	"scripts": {
		"build": "node build.mjs"
	},
	"dependencies": {
		"@xterm/addon-fit": "^0.11.0",
		"@xterm/xterm": "^6.0.0"
	},
	"devDependencies": {
		"esbuild": "^0.25.5",
		"ssh2": "^1.11.0"
	}
}
